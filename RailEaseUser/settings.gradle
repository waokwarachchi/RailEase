dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter()
        maven { url "https://maven.xyz.com" }// Warning: this repository is going to shut down soon
    }
}

rootProject.name = "Computing project"
include ':app'
